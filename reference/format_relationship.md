# Format a relationship tree as a human-readable string

Format a relationship tree as a human-readable string

## Usage

``` r
format_relationship(tree)
```

## Arguments

- tree:

  A parsed relationship tree.

## Value

A single string.
